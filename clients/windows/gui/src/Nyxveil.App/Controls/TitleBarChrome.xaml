<UserControl x:Class="Nyxveil.App.Controls.TitleBarChrome"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="36" Background="{StaticResource TitleBar}"
             UseLayoutRounding="True" SnapsToDevicePixels="True">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <Border x:Name="DragArea" Grid.Column="0" Background="Transparent"
            MouseLeftButtonDown="DragArea_OnMouseLeftButtonDown">
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
        <Image Source="{StaticResource IconMoon}" Width="14" Height="14" Margin="0,0,8,0"/>
        <TextBlock Text="Nyxveil" FontSize="12" FontWeight="SemiBold"
                   Foreground="{StaticResource FgPrimary}" VerticalAlignment="Center"/>
      </StackPanel>
    </Border>

    <StackPanel Grid.Column="1" Orientation="Horizontal">
      <Button Style="{StaticResource ChromeButton}" Click="Minimize_OnClick" ToolTip="Свернуть">
        <TextBlock Text="─" FontSize="12"/>
      </Button>
      <Button Style="{StaticResource ChromeButton}" Click="Maximize_OnClick" ToolTip="Развернуть">
        <TextBlock Text="□" FontSize="12"/>
      </Button>
      <Button Style="{StaticResource ChromeCloseButton}" Click="Close_OnClick" ToolTip="Закрыть">
        <TextBlock Text="✕" FontSize="11"/>
      </Button>
    </StackPanel>
  </Grid>
</UserControl>
