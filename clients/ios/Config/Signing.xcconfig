// Override in Local.xcconfig (ignored by git). No Apple Team is assumed.
DEVELOPMENT_TEAM =
APP_BUNDLE_IDENTIFIER = ru.REPLACE.nyxveil
TUNNEL_BUNDLE_IDENTIFIER = $(APP_BUNDLE_IDENTIFIER).tunnel
APP_GROUP_IDENTIFIER = group.$(APP_BUNDLE_IDENTIFIER)
KEYCHAIN_GROUP_IDENTIFIER = $(AppIdentifierPrefix)$(APP_BUNDLE_IDENTIFIER)
#include? "Local.xcconfig"
